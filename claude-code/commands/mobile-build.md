---
description: Build the mobile project (React Native, Expo, Flutter)
argument-hint: "Build target (e.g., 'ios', 'android', 'both', 'eas build')"
---

# Build Mobile Project

Build the mobile project and report results.

## Arguments
- `$ARGUMENTS` — Optional build target or platform.

## Instructions
You are an orchestrator. Plan and delegate.

## Phase 1: Understand Context
1. Read CLAUDE.md, identify framework (React Native, Expo, Flutter)
2. Read `package.json` / `pubspec.yaml` for build scripts
3. Determine build command

## Phase 2: Build
Launch `mobile-builder` with build instructions. Fix errors (max 2 rounds).

## Phase 3: Report
Present: build status, platform notes, errors/warnings.
